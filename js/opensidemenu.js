/* Set the width of the sidebar to 250px (show it) */
function openNav() {
    document.getElementById("togglemenu").style.width = "50%";
  }
  
  /* Set the width of the sidebar to 0 (hide it) */
  function closeNav() {
    document.getElementById("togglemenu").style.width = "0";
  }